{
  "name": "reflectapp",
  "version": "1.0.0",
  "description": "Sample service for ECS Fargate zero-traffic pipeline demo",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:dev": "NODE_ENV=development nodemon index.js",
    "lint": "npx eslint . || true"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.360.0",
    "express": "^4.18.2",
    "pino": "^8.13.0",
    "pino-pretty": "^10.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "eslint": "^8.47.0"
  }
}
